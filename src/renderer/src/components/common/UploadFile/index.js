import React from 'react';
import { FileUploader } from "react-drag-drop-files";

const UploadFile = () => {
    return (
        <div>
            
        </div>
    );
};

export default UploadFile;